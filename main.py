# main.py — точка входа в игру

# Импортируем главный класс Game из модуля core.game
from core.game import Game

# Проверяем, что скрипт запущен напрямую (не импортирован)
if __name__ == "__main__":
    # Создаём объект игры
    game = Game()
    # Запускаем главный цикл
    game.run()